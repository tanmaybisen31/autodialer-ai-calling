<!DOCTYPE html>
<html>
<head>
  <title>Autodialer - AI-Powered Call System</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Typography -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --color-bg: #05070f;
      --color-surface: #0f172a;
      --color-surface-elevated: #111d33;
      --color-border: #1f2937;
      --color-text: #e2e8f0;
      --color-muted: #94a3b8;
      --color-primary: #38bdf8;
      --color-primary-soft: rgba(56, 189, 248, 0.18);
      --color-danger: #f87171;
      --color-danger-soft: rgba(248, 113, 113, 0.16);
      --shadow-soft: 0 18px 40px rgba(2, 6, 23, 0.55);
    }

    body {
      background: radial-gradient(circle at top, rgba(37, 99, 235, 0.08), transparent 55%), var(--color-bg);
      font-family: 'Inter', 'Segoe UI', sans-serif;
      color: var(--color-text);
      min-height: 100vh;
      margin: 0;
      line-height: 1.6;
    }

    a {
      color: var(--color-primary);
      text-decoration: none;
    }

    a:hover {
      color: #7dd3fc;
    }

    .navbar {
      background: linear-gradient(130deg, var(--color-surface) 0%, var(--color-surface-elevated) 100%) !important;
      border-bottom: 1px solid var(--color-border);
      box-shadow: 0 12px 35px rgba(2, 6, 23, 0.4);
    }

    .navbar-brand {
      color: var(--color-text) !important;
      font-weight: 600;
      letter-spacing: 0.03em;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .navbar-brand i {
      color: var(--color-primary);
    }

    .navbar-toggler {
      border: none;
      background: rgba(148, 163, 184, 0.12);
    }

    .navbar-toggler:focus {
      box-shadow: 0 0 0 2px var(--color-primary-soft);
    }

    .nav-link {
      color: var(--color-muted) !important;
      font-weight: 500;
      margin-left: 0.75rem;
      transition: color 0.2s ease;
    }

    .nav-link:hover,
    .nav-link:focus,
    .nav-link.active {
      color: var(--color-primary) !important;
    }

    .nav-link i {
      margin-right: 0.35rem;
    }

    main.container {
      max-width: 1140px;
      padding-top: 32px;
      padding-bottom: 56px;
      position: relative;
      z-index: 1;
    }

    .card {
      background: linear-gradient(160deg, var(--color-surface) 0%, rgba(17, 24, 39, 0.9) 100%);
      border: 1px solid var(--color-border);
      border-radius: 16px;
      box-shadow: var(--shadow-soft);
      margin-bottom: 24px;
      transition: box-shadow 0.2s ease, transform 0.2s ease;
    }

    .card:hover {
      transform: translateY(-2px);
      box-shadow: 0 16px 35px rgba(15, 23, 42, 0.12);
    }

    .card-header {
      background-color: transparent !important;
      border-bottom: 1px solid var(--color-border) !important;
      color: var(--color-text);
      font-weight: 600;
      letter-spacing: 0.02em;
      padding: 20px 24px;
    }

    .card-body {
      padding: 24px;
      color: var(--color-text);
    }

    .stat-card {
      text-align: center;
      padding: 24px;
      background: linear-gradient(160deg, var(--color-surface) 0%, rgba(17, 24, 39, 0.92) 100%);
      border: 1px solid var(--color-border);
      border-radius: 18px;
      color: var(--color-text);
    }

    .stat-card h3 {
      font-size: 2.2rem;
      font-weight: 600;
      margin: 0;
      color: var(--color-primary);
    }

    .stat-card p {
      color: var(--color-muted);
      margin-top: 8px;
      text-transform: uppercase;
      font-size: 0.76rem;
      letter-spacing: 0.12em;
    }

    .stat-card--info {
      border-color: rgba(56, 189, 248, 0.35);
      box-shadow: 0 12px 28px rgba(56, 189, 248, 0.12);
    }

    .stat-card--info h3 {
      color: #38bdf8;
    }

    .stat-card--warning {
      border-color: rgba(249, 115, 22, 0.35);
      box-shadow: 0 12px 28px rgba(249, 115, 22, 0.12);
    }

    .stat-card--warning h3 {
      color: #fb923c;
    }

    .stat-card--primary {
      border-color: rgba(59, 130, 246, 0.35);
      box-shadow: 0 12px 28px rgba(59, 130, 246, 0.12);
    }

    .stat-card--primary h3 {
      color: #60a5fa;
    }

    .stat-card--success {
      border-color: rgba(34, 197, 94, 0.35);
      box-shadow: 0 12px 28px rgba(34, 197, 94, 0.12);
    }

    .stat-card--success h3 {
      color: #4ade80;
    }

    .stat-card--danger {
      border-color: rgba(248, 113, 113, 0.35);
      box-shadow: 0 12px 28px rgba(248, 113, 113, 0.12);
    }

    .stat-card--danger h3 {
      color: #f87171;
    }

    .stat-card--neutral {
      border-color: rgba(148, 163, 184, 0.28);
      box-shadow: 0 12px 28px rgba(148, 163, 184, 0.1);
    }

    .stat-card--neutral h3 {
      color: #cbd5f5;
    }

    .phone-list,
    .call-log {
      max-height: 420px;
      overflow-y: auto;
    }

    .ai-command-box {
      background: linear-gradient(160deg, var(--color-surface) 0%, rgba(17, 24, 39, 0.9) 100%);
      border: 1px solid var(--color-border);
      color: var(--color-text);
      padding: 32px;
      border-radius: 18px;
      box-shadow: var(--shadow-soft);
      transition: box-shadow 0.2s ease, transform 0.2s ease;
    }

    .ai-command-box:hover {
      transform: translateY(-2px);
      box-shadow: 0 16px 35px rgba(15, 23, 42, 0.12);
    }

    .list-group {
      background-color: transparent;
      border-radius: 14px;
    }

    .list-group-item {
      background-color: rgba(15, 23, 42, 0.75);
      border: 1px solid rgba(148, 163, 184, 0.18);
      color: var(--color-text);
      transition: background-color 0.2s ease, transform 0.2s ease;
    }

    .list-group-item + .list-group-item {
      border-top-width: 1px;
    }

    .list-group-item:hover {
      background-color: rgba(30, 41, 59, 0.95);
      transform: translateX(2px);
    }

    .list-group-item .text-muted {
      color: var(--color-muted) !important;
    }

    .list-group-item .badge {
      box-shadow: none;
    }

    .form-control,
    .form-select {
      background-color: rgba(15, 23, 42, 0.92);
      border: 1px solid rgba(148, 163, 184, 0.25);
      color: var(--color-text);
      border-radius: 10px;
      padding: 10px 14px;
    }

    .form-control:focus,
    .form-select:focus {
      background-color: rgba(15, 23, 42, 1);
      border-color: var(--color-primary);
      box-shadow: 0 0 0 3px var(--color-primary-soft);
      color: var(--color-text);
    }

    .form-control::placeholder {
      color: rgba(148, 163, 184, 0.6);
    }

    .btn {
      border-radius: 10px;
      font-weight: 600;
      letter-spacing: 0.02em;
      padding: 10px 18px;
      transition: transform 0.2s ease, background-color 0.2s ease, border-color 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
    }

    .btn-primary {
      background: linear-gradient(145deg, var(--color-primary) 0%, #0ea5e9 100%);
      border: 1px solid transparent;
    }

    .btn-primary:hover {
      background: linear-gradient(145deg, #0ea5e9 0%, #0284c7 100%);
      border-color: transparent;
    }

    .btn-success {
      background: linear-gradient(145deg, #22c55e 0%, #16a34a 100%);
      border: 1px solid transparent;
    }

    .btn-success:hover {
      background: linear-gradient(145deg, #16a34a 0%, #15803d 100%);
      border-color: transparent;
    }

    .btn-danger {
      background: linear-gradient(145deg, var(--color-danger) 0%, #ef4444 100%);
      border: 1px solid transparent;
    }

    .btn-danger:hover {
      background: linear-gradient(145deg, #ef4444 0%, #b91c1c 100%);
      border-color: transparent;
    }

    .badge {
      text-transform: uppercase;
      letter-spacing: 0.08em;
      padding: 0.42rem 0.7rem;
      border-radius: 999px;
      font-weight: 600;
      font-size: 0.7rem;
    }

    .badge.bg-success {
      background-color: var(--color-primary-soft) !important;
      color: var(--color-primary);
      border: none;
    }

    .badge.bg-danger {
      background-color: var(--color-danger-soft) !important;
      color: var(--color-danger);
      border: none;
    }

    .badge.bg-warning {
      background-color: rgba(249, 115, 22, 0.18) !important;
      color: #fb923c;
      border: none;
    }

    .badge.bg-info {
      background-color: rgba(14, 165, 233, 0.18) !important;
      color: #38bdf8;
      border: none;
    }

    .badge.bg-secondary {
      background-color: rgba(148, 163, 184, 0.25) !important;
      color: var(--color-text);
      border: none;
    }

    .table {
      color: var(--color-text);
    }

    .table th {
      color: var(--color-muted);
      border-color: var(--color-border);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      font-size: 0.75rem;
      font-weight: 600;
      background-color: rgba(15, 23, 42, 0.7);
    }

    .table td {
      border-color: var(--color-border);
      vertical-align: middle;
    }

    .phone-table-wrapper {
      max-height: 420px;
      overflow-y: auto;
      border-radius: 18px;
      border: 1px solid var(--color-border);
      background: rgba(15, 23, 42, 0.55);
      box-shadow: inset 0 1px 0 rgba(148, 163, 184, 0.06);
    }

    .phone-table-wrapper .table {
      margin-bottom: 0;
      border-collapse: separate;
      border-spacing: 0;
    }

    .phone-table-wrapper thead th {
      position: sticky;
      top: 0;
      z-index: 2;
      background: rgba(8, 15, 30, 0.95);
      backdrop-filter: blur(6px);
    }

    .phone-table-wrapper tbody tr {
      background-color: rgba(15, 23, 42, 0.7);
      transition: background-color 0.2s ease, transform 0.2s ease;
    }

    .phone-table-wrapper .table tbody tr td {
      background-color: transparent !important;
      color: var(--color-text);
      border-color: rgba(148, 163, 184, 0.12);
    }

    .phone-table-wrapper tbody tr + tr td {
      border-top: 1px solid rgba(148, 163, 184, 0.12);
    }

    .phone-table-wrapper tbody tr:hover {
      background-color: rgba(56, 189, 248, 0.12);
      transform: translateX(2px);
    }

    .phone-table-wrapper .table-hover tbody tr:hover td {
      background-color: transparent;
      color: var(--color-text);
    }

    .phone-table-wrapper::-webkit-scrollbar {
      width: 8px;
    }

    .phone-table-wrapper::-webkit-scrollbar-track {
      background: rgba(15, 23, 42, 0.6);
      border-radius: 10px;
    }

    .phone-table-wrapper::-webkit-scrollbar-thumb {
      background: rgba(56, 189, 248, 0.35);
      border-radius: 10px;
    }

    .phone-table-wrapper::-webkit-scrollbar-thumb:hover {
      background: rgba(56, 189, 248, 0.5);
    }

    .progress {
      background-color: rgba(15, 23, 42, 0.6);
      border-radius: 999px;
      height: 0.55rem;
      overflow: hidden;
    }

    .progress-bar {
      background: linear-gradient(90deg, var(--color-primary) 0%, #0ea5e9 100%);
      box-shadow: 0 0 12px rgba(56, 189, 248, 0.25);
    }

    .pagination {
      gap: 0.25rem;
    }

    .pagination .page-link {
      background-color: rgba(15, 23, 42, 0.7);
      border: 1px solid var(--color-border);
      color: var(--color-text);
      border-radius: 10px;
      transition: background-color 0.2s ease, border-color 0.2s ease, color 0.2s ease;
    }

    .pagination .page-link:hover {
      background-color: rgba(56, 189, 248, 0.16);
      border-color: rgba(56, 189, 248, 0.4);
      color: var(--color-primary);
    }

    .pagination .page-item.active .page-link {
      background-color: var(--color-primary);
      border-color: var(--color-primary);
      color: #020617;
    }

    .pagination .page-item.disabled .page-link {
      background-color: rgba(15, 23, 42, 0.35);
      border-color: rgba(148, 163, 184, 0.18);
      color: rgba(148, 163, 184, 0.45);
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      color: var(--color-text);
      font-weight: 600;
    }

    .text-muted {
      color: var(--color-muted) !important;
    }

    @media (max-width: 767px) {
      .navbar-brand {
        font-size: 1rem;
      }

      .stat-card {
        padding: 18px;
      }

      .ai-command-box {
        padding: 24px;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <i class="fas fa-phone-volume"></i> Autodialer
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">
              <i class="fas fa-home"></i> Home
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/dashboard">
              <i class="fas fa-chart-line"></i> Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/blogs">
              <i class="fas fa-blog"></i> Blog
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container mt-4">
    <%= yield %>
  </main>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</body>
</html>
